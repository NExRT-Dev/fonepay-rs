# fonepay-dynamic-qr

A Rust crate for integrating with **Fonepay Dynamic QR** APIs.  
Implements QR request generation, HMAC-SHA512 signing, status checking, and WebSocket listening.

## Installation

Add to your project:

```bash
cargo add fonepay-dynamic-qr